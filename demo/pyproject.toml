[project]
name = "risktrace-demo"
version = "0.1.0"
description = "RiskTrace historical source conversion and replay processor"
requires-python = ">=3.13,<3.14"
dependencies = []

[tool.uv]
package = false

