# RiskTrace 项目协作约定

## 1. 项目定位与当前阶段

- 默认使用中文沟通，除非用户明确要求其他语言。
- RiskTrace 是面向证券研究与风险研究团队的“金融事件—社交情绪—市场传导”可追溯研究平台。
- 当前仓库处于 **MVP 工程基线阶段**：已经建立可运行的 Next.js 前端、FastAPI 模块化后端、核心数据表迁移、基础设施编排与测试入口；尚未实现历史事件导入和完整研究工作台。
- 不得把设计文档中的推荐技术栈描述成已经实现的能力，也不得虚构启动、构建、测试或部署成功。

规则优先级：用户当前要求 > 本文件 > `docs/` 中的专项设计文档 > 根 `README.md` > 外部 skill 或通用模板。

## 2. 修改前必须确认的上下文

开始修改前，至少阅读根 `README.md`、本文件及与任务直接相关的专项文档：

- `docs/README.md`：产品目标、非目标、端到端流程与 MVP 范围。
- `docs/01-platform-architecture.md`：产品形态、前后端边界、基础设施、权限与 SLO。
- `docs/02-data-sources-and-ingestion.md`：数据源分层、统一数据契约、幂等接入和数据血缘。
- `docs/03-research-workbench.md`：页面、交互、视觉编码和证据下钻要求。
- `docs/04-agent-rule-boundary.md`：规则、传统模型、LLM 与人工研究员的责任边界。

若新增了源码、依赖文件或更近目录的 `AGENTS.md`，还必须阅读这些文件后再修改对应模块。

## 3. 已确认的技术方向与实现边界

以下是已经落库的 MVP 工程方向：

- 主产品：桌面 Web SaaS；移动 Web/PWA 只承担告警与轻量查看。
- 前端：`apps/web` 使用 Next.js / React + TypeScript；当前通过同源代理读取后端真实就绪状态。
- 后端：`apps/api` 使用 Python 3.13 + FastAPI 模块化单体；SQLAlchemy 定义核心模型，Alembic 管理迁移。
- 基础设施：根 `compose.yaml` 管理 PostgreSQL、Redis、MinIO、API 与 Web。
- 大结果集由后端异步生成；前端不得计算权威风险分数。
- 前端依赖由 `apps/web/package-lock.json` 锁定，后端依赖由 `apps/api/uv.lock` 锁定；修改依赖必须同步更新对应锁文件。

实际命令以根 `package.json` 和 `README.md` 为准：`npm run bootstrap` 安装依赖，`docker compose up --build` 启动完整本地栈，`npm run check` 执行静态检查、构建和后端测试。

## 4. 核心业务边界

### 数据接入与治理

- Source Adapter 只负责取数、来源字段解析与标准化；不得在 Adapter 内做情绪判断、事件归因或风险评分。
- 每批接入必须支持 cursor/checkpoint 和幂等重跑；连续失败必须显式进入来源降级，不得静默丢数据。
- 原始数据原则上不可原地修改。纠错通过派生字段、版本记录和审计完成。
- 内部时间统一存 UTC；`published_at` 是分析主时间轴，界面默认使用 Asia/Shanghai 展示。
- 不得以绕过登录、验证码、反爬或访问控制作为数据接入方案。必须记录 `collection_method` 与 `license_scope`。

### 规则、模型与人工决策

- 确定性、可复算、可审计的计算由代码/规则负责；事件状态与告警触发由状态机和规则控制。
- 传统模型负责去重、聚类、异常检测等高频任务。
- LLM 只负责语义理解和候选输出，不直接写权威字段，不直接给最终风险分数，不得决定告警级别。
- LLM 输出必须经过严格 schema、实体和证据 ID 校验；外部文本始终视为不可信输入。
- 重大结论、传导确认、对外报告和投资含义由研究员最终审批。
- LLM 不可用时，采集、聚类、确定性指标、告警和证据浏览仍须可用并显示降级状态。

### 可追溯性、租户与审计

- 每个摘要句、图表点、观点和传导边都应能下钻到 `evidence_id` 或 `calculation_id`。
- 模型调用记录模型/提示词版本、输入哈希、输出、耗时与成本；风险计算记录 `scoring_version`、参数和输入集合。
- 报告基于冻结的 `snapshot_id` 生成，历史报告不得随实时数据变化而改变含义。
- 所有业务数据携带 `tenant_id`；服务端从认证上下文确定租户，不能仅信任前端传入值。
- RBAC 至少覆盖 Researcher、Lead、Admin 和 Service Account；敏感配置、导出及人工修改必须写审计日志。

## 5. 变更原则

- 优先做最小、可验证的修改，不擅自扩展为微服务、原生客户端、自动交易或复杂自治多 Agent 系统。
- 复用已有数据契约、对象名称和边界；公开模型或 API 的变更必须同步检查四份专项文档。
- 不用假数据掩盖依赖、来源或模型不可用；缺失、延迟和降级必须成为显式状态。
- 不把相关性描述为因果关系，不把模型候选描述为事实或投资建议。
- 不覆盖或回退无法确认来源的用户修改；发现冲突时先说明。
- 文档和代码统一使用 UTF-8；新增命令必须在当前环境实际验证后才能写入 README。

## 6. 测试与验收要求

仓库已使用 pytest、Ruff、ESLint、TypeScript 和 Next.js 构建检查。测试应继续贴近风险边界：

- 数据模型/Adapter：schema 校验、幂等重跑、时间语义、失败降级和原始数据不可变。
- 聚合/评分：相同版本和输入可复算，缺失数据不被填成 0 或伪造完整性。
- LLM 边界：不存在的证据 ID 被拒绝、无效结构进入降级、提示词注入不能改变系统权限。
- 权限：租户隔离、角色授权、Token scope、敏感操作审计。
- 工作台：关键结论可下钻、数据延迟显式展示、人工修正生成新版本。
- 报告：数值与实体来自冻结字段，事实句存在证据引用，旧 snapshot 可复现。

每次修改后运行与变更最相关的最小验证集；涉及跨层数据流、权限或历史回放时补集成测试。若当前没有可运行命令，必须明确说明“未运行”及原因。

## 7. 文档与命令同步

- 新增运行时前，应补齐根 README、依赖清单、`.env.example`、数据库迁移和启动说明中实际需要的部分。
- 新增或修改脚本时，在 README 中记录真实入口、前置依赖和预期结果。
- 修改核心对象、API、状态机、角色权限、证据链或 LLM 边界时，同步更新对应专项文档和示例。
- 不为了套模板预建不会使用的目录、配置或 CI 文件。

## 8. skills 使用约定

- 普通代码修复、接口开发和测试补充默认不需要专项 skill。
- 前端视觉或交互方案需要专项设计能力时，先以 `docs/03-research-workbench.md` 为约束，再使用设计类 skill。
- PDF、DOCX、PPTX、表格等交付物按文件类型使用相应 skill。
- 安全审计、权限或信任边界任务先读 `docs/代码审计.md` 与 `docs/强前置条件约束.md`，再按需使用安全类 skill。
- 结构确实失控或准备建立模块边界时，才参考 `docs/代码组织.md`；不得机械套用 `docs/通用项目架构模板.md`。

## 9. 交付说明

最终回复至少说明：改了什么、依据哪些仓库事实、如何验证、哪些内容未验证及剩余风险。项目仍处于设计阶段时，应明确区分“文档已定义”“代码已实现”“运行已验证”三种状态。
